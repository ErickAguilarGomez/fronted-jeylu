<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirigir todas las peticiones que no sean archivos ni directorios a index.html (Soporte para Vue Router)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
